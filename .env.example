# 🌐 ENVIRONMENT
ENVIRONMENT=development

# 🚀 APP
PROJECT_NAME="Kura"
VERSION="1.0.0"
API_V1_PREFIX=/api/v1
DEBUG=true

DATABASE_URL=postgresql://user:password@localhost:5432/kura
DB_HOST='localhost'
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=kura
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# 🔐 SECURITY
SECRET_KEY=your_secret_key_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 📊 LOGGING
LOG_LEVEL=DEBUG
LOG_FORMAT=default
ENABLE_SQL_LOGGING=false

# 🌍 CORS
CORS_ORIGINS=["*"]
CORS_ALLOW_CREDENTIALS=true

# ⚡ PERFORMANCE
REQUEST_TIMEOUT=30

# JWT Configuration
JWT_SECRET=your_secret_key_here
JWT_ALG=HS256
JWT_AUDIENCE=kura-api
JWT_ISSUER=https://idp.local
ACCESS_TOKEN_EXPIRE_SECONDS=3600

# WORM Configuration
WORM_PRIVATE_KEY=para crearla debe correr el comando python3 scripts/auth/generate_worm_key.py y copiar la clave privada generada
WORM_VERSION=1.0
WORM_ISSUER=Alias Chile

# Security
CUB_PEPPER=your_pepper_here